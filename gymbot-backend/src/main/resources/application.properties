# GYMBot Spring Boot Application Configuration

# Server configuration
server.port=8080
server.servlet.context-path=/

# Application name
spring.application.name=GYMBot

# Logging level
logging.level.root=INFO
logging.level.com.gymbot=DEBUG

# Data storage path (relative to project root)
data.storage.path=data/
